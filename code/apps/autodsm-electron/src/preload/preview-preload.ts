// Sprint 6 (M6) wires this preload to bridge the Storybook preview iframe's
// channel events into Electron IPC. For Sprint 1 we ship the file so the
// WebContentsView preload path resolves cleanly.

export {};
